name: CI - DEV

on:
  push:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: CI marker
        run: echo "CI - DEV completed successfully"
